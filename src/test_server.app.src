{application, test_server,
    [{description, "An OTP application"},
        {vsn, "0.1.0"},
        {registered, []},
        {mod, {test_server_app, []}},
        {applications,
            [
                kernel,
                stdlib,
                sasl,
                erl_gs_db,
                ranch,
                lager,
                cowboy,
                eredis_pool
            ]},
        {env, [{http_port, 8081}, {cfg_path, "./config"}, {tcp_port, 7621}, {db_cache_tactics, write_behind}]},
        {modules, []},

        {licenses, ["Apache 2.0"]},
        {links, []}
    ]}.
